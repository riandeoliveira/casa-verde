generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Plant {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  name String
  price Float
  image_url String

  @@map("plants")
}
