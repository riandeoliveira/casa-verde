export interface ColorPallete {
  primary: string;
  primary_light: string;
  secondary: string;
  tertiary: string;
  neutral: string;
  neutral_light: string;
}

export interface TextFonts {
  primary: string;
  secondary: string;
  default: string;
}
