interface TextFonts {
  default: string;
  primary: string;
  secondary: string;
}

const fonts: TextFonts = {
  primary: "Elsie Swash Caps",
  secondary: "Montserrat",
  default: "sans-serif",
};

export default fonts;
